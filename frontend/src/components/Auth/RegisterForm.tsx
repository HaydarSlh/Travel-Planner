// Controlled form for new account creation.
// Calls POST /auth/register, then automatically calls POST /auth/login on success.
// Shows inline validation errors (email format, password strength) before submission.
